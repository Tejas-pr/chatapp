# reference -> https://github.com/calcom/cal.com/blob/main/.github/workflows/all-checks.yml \
# https://github.com/appleboy/ssh-action

name: CI/CD Pipeline

on:
  push:
    branches: 
      - staging
      - master
  pull_request:
    branches: 
      - staging

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/staging'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build app
        run: pnpm run build

  deploy:
    runs-on: ubuntu-latest
    needs: [build-and-test]
    if: github.ref == 'refs/heads/master'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.GITHUB_USER }}
          key: ${{ secrets.GITHUB_SSH_KEY }}
          script: |
            cd chatapp
            git pull origin master
            pnpm install
            cd packages/db
            npx prisma migrate deploy
            cd ../../
            pnpm run build
            pm2 restart chatapp || pm2 start pnpm --name "chatapp" -- run dev
